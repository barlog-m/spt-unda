name: .NET 9 CI

env:
  PROJECT_NAME: 'BarlogM-Unda'

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Build
        run: |
          cd ${{ env.PROJECT_NAME }}
          dotnet build --configuration Release --output dist

      - name: Create zip archive
        run: |
          cd ${{ env.PROJECT_NAME }}
          mv dist ${{ env.PROJECT_NAME }}
          zip -r ${{ env.PROJECT_NAME }}.zip ${{ env.PROJECT_NAME }}

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ${{ env.PROJECT_NAME }}/${{ env.PROJECT_NAME }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
